services:
  knowwhere:
    build: .
    ports:
      - "3000:3000"
    environment:
      - RUST_LOG=info
      - KNOWWHERE_API_KEY=${KNOWWHERE_API_KEY:-}
    # depends_on:
    #   - redis

  # Redis fuer spaetere Rate-Limits (optional, auskommentiert fuer MVP)
  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"
